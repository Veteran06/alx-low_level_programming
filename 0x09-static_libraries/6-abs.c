#include "main.h"

/**
 * _abs - computes the absolute value of an integer.
 * @q: int to check
 * Return: absolute value of int
 */
int _abs(int q)
{
        if (q >= 0)
        {
                return (q);
        }
        return (-q);
}
