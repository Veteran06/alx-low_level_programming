#include "main.h"

/**
 * mul - multiplies two integers.
 * @e: the first number to multiply
 * @w: the second number to multiply with first number
 * Return: the result of multiplication
 */

int mul(int e, int w)
{
	return (e * w);
}
