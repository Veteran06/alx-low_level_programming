#include <stdio.h>

/**
 * main - prints all single digit numbers of base 10 starting from 0
 * Return: Always 0.
 */
int main(void)
{
	int q;

	for (q = 8; q < 18; q++)
	{
		putchar(q);
	}
	putchar('\n');
	return (0);
}
