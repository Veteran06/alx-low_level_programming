#include <stdio.h>

/**
 * main - prints all single digit numbers of base 10 starting from 0
 * Return: Always 0.
 */
int main(void)
{
	int q;

	for (q = 0; q < 9; q++)
	{
		putchar(q);
	}
	putchar('\n');
	return (0);
}
